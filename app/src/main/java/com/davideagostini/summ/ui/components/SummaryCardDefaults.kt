package com.davideagostini.summ.ui.components

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

object SummaryCardDefaults {
    val outerPadding = PaddingValues(horizontal = 20.dp, vertical = 8.dp)
    val contentPadding = PaddingValues(horizontal = 18.dp, vertical = 18.dp)
    val shape = RoundedCornerShape(30.dp)
    val titleSpacing = 12.dp
    val sectionSpacing = 12.dp
    val metricRowSpacing = 12.dp
}
